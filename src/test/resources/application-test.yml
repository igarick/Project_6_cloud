spring:
  application:
    name: pet_6-test

  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
    enabled: true


logging:
  level:
    org.hibernate.sql: debug

minio:
  bucket: test-bucket
