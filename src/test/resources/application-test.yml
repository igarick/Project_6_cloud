spring:
  application:
    name: pet_6-test

  datasource:
    driver-class-name: org.postgresql.Driver
  #    url: ""
#    username: ""
#    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
#    enabled: false
    enabled: true
#    default-schema: project_schema
#    liquibase-schema: project_schema


logging:
  level:
    org.hibernate.sql: debug

minio:
##  endpoint: http://localhost:9000
  bucket: test-bucket
